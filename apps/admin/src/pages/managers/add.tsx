import {Layout} from '@/components/layout';
import {CreateUpdateManagerForm} from '@/features/CreateUpdateManagerForm';

export default function CreateBusinessPage() {
  return (
    <Layout>
      <CreateUpdateManagerForm />
    </Layout>
  );
}
